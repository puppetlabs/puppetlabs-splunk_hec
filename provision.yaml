acceptance:
  provisioner: abs
  images: ['ubuntu-1804-x86_64']
  vars: |
    role: server
fips_acceptance:
  provisioner: abs
  images:
  - redhat-fips-7-x86_64
  - ubuntu-1804-x86_64
fips_acceptance_testing:
  provisioner: abs
  images:
  - centos-7-x86_64
  - centos-7-x86_64
